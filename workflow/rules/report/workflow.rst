Analysis steps and rules for the Microbiome Analysis Workflow


This report contains the results for the samples located in this directory "{{ snakemake.config["input_directory"] }}" and analysis was performed using the conda environment "{{ snakemake.config["conda_envs"] }}"
